<tr>
  <td>
    <%= link_to question.body, admin_question_path(question)%>
  </td>
  <td>
    <%= link_to t('edit'), edit_admin_question_path(question), class: 'btn btn-secondary' %>
  </td>
  <td>
    <%= link_to t('delete'), admin_question_path(question), method: :delete, data: { confirm: t('confirm_deletion') }, class: 'btn btn-secondary' %>
  </td>
</tr>

